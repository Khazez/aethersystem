/**
 * Реквизиты компании и состав руководства.
 *
 * Почему здесь, а не в словарях: почта, телефон и имена людей на разных
 * языках не меняются. Если положить их в три словаря, при первой же
 * правке копии разойдутся, и на одном из языков останется старый номер.
 * Переводятся только должности — они и лежат в словарях, привязанные
 * к `id` человека.
 *
 * Данные получены от заказчика 27.08.2026. Ничего не додумано:
 * чего не прислали (адрес) — того здесь нет, и на странице честно стоит
 * «будет опубликовано».
 */

/** Электронная почта компании. Единственный способ связи: форм на сайте нет. */
export const EMAIL = "info@aethersystem.kz";

/**
 * Адрес платформы Aether Nexus — самого продукта, а не страницы с его
 * описанием. Живёт на поддомене и на отдельном сервере, к сайту
 * отношения не имеет: сайт про компанию, платформа про работу.
 *
 * Здесь, а не в словарях: адрес один на все языки. Подпись кнопки
 * переводится и лежит в словарях под ключом `nav.platform`.
 */
export const PLATFORM_URL = "https://platform.aethersystem.kz";

/** Телефон: как показывать людям и как отдавать в ссылку для звонка. */
export const PHONE = {
  /** Для чтения глазами — с пробелами по разрядам. */
  display: "+7 700 832 32 73",
  /** Для ссылки `tel:` — без пробелов, иначе часть телефонов её не поймёт. */
  href: "+77008323273",
};

/**
 * Руководство компании.
 *
 * `id` связывает человека с его должностью в словарях — так должность
 * переводится, а имя остаётся одно на все языки.
 *
 * `nameLatin` — написание для английской версии. **Подтверждено
 * заказчиком 27.08.2026**, менять только по его слову: написание
 * собственного имени человек выбирает сам.
 * Отчество в английской версии опущено намеренно: в англоязычной
 * деловой практике его не пишут.
 */
export const LEADERSHIP = [
  {
    id: "director",
    name: "Бекмухамбетов Даулет Айдарканович",
    nameLatin: "Daulet Bekmukhambetov",
  },
  {
    id: "deputyTechnical",
    name: "Худайбергенов Берикжан Талгатович",
    nameLatin: "Berikzhan Khudaibergenov",
  },
  {
    id: "deputyFinance",
    name: "Рахимгалиев Азат Слямгазыұлы",
    nameLatin: "Azat Rakhimgaliyev",
  },
] as const;

/** Идентификатор человека — по нему в словарях лежит его должность. */
export type LeadershipId = (typeof LEADERSHIP)[number]["id"];
